ROLE:
You are the implementation agent for GEM Cybersecurity Assist.
Apply the UPDATED social blueprint: replace LinkedIn with Facebook + Nextdoor.
Do NOT break or redesign existing services (backend, /api/news, dashboard auto-feed, Telegram).

NEW DISTRIBUTION LAYER:
Primary: Ayrshare API (single port for facebook, instagram, twitter/x, tiktok, nextdoor).
Secondary optional: Meta Business Suite API (ONLY if asked later; higher maintenance).

KEY STRATEGY SHIFT:
- Facebook + Nextdoor are community-centric.
- Content tone must be personal, local, utility-focused (saving money, safe spending, family budgeting).
- Avoid crypto slang for Nextdoor (no “HODL”, “bullish”, “moon”, etc.).
- Use: “Savings”, “Secure”, “Easy”, “Fees”, “Budget”, “Neighbors”, “Local tip”.

TRIGGERS:
A) New Card Feature
B) Daily Market Summary (if used, keep non-spammy on Nextdoor)
C) Inflation/fees/consumer-cost events (“Inflation Buster” content)
D) Visual verification post (anonymized “virtual card created” activity)

CONTENT GENERATION (LLM MUST OUTPUT 3 VERSIONS):
1) Version A (X/Twitter): short, punchy, market-focused, light hashtags.
2) Version B (Facebook/Insta): casual, shareable, family budgeting/ease-of-use tone.
3) Version C (Nextdoor): “Hello neighbors…” helpful tip tone, no crypto slang, local utility framing.

DISTRIBUTION (Ayrshare):
- Post via a single API call with explicit platform flags.
- Default platforms: ["facebook","instagram","twitter","tiktok","nextdoor"]
- For Facebook: use Reels for video when available (isReel: true).
- For Nextdoor: business page scope must be set (scope: "public" or required setting).

ANTI-SPAM / QUALITY RULES (Nextdoor + Facebook):
- Nextdoor: No hype, no exaggerated claims, no “viral” language, no excessive hashtags.
- Always include 1 clear “helpful tip” sentence.
- Keep Nextdoor text short (2–5 sentences), community-friendly, optional light local reference.
- No more than 1 link (prefer your own domain).
- Post frequency limits: Nextdoor max 1 per day by default unless explicitly changed.

DELIVERABLES:
1) Implement platform renderers:
   - render_x(article/event) -> text
   - render_facebook(article/event) -> caption
   - render_nextdoor(article/event) -> neighbor-friendly caption
2) Implement Ayrshare publisher wrapper:
   - publish_to_ayrshare(post_text, platforms, mediaUrls, options)
3) Implement dedupe:
   - stable post key = platform + article.id (or hash(url))
4) Implement per-platform logs:
   - success/failure isolated per platform; must never impact Telegram/website/dashboard.

DO NOT CHANGE:
- Existing dashboard continuous auto-feed behavior
- Existing Telegram behavior
- Existing API routes and domain